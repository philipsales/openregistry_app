import { QuantityDateValidationDirective } from './quantity-date-validation.directive';

describe('QuantityDateValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new QuantityDateValidationDirective();
    expect(directive).toBeTruthy();
  });
});
